export enum OutputFormat {
	DATAFRAME = "dataframe",
	INTERNAL = "internal",
	JSON = "json",
	CSV = "csv",
}

export enum DateFormat {
	TIMESTAMP = "timestamp",
	UNIX = "unix",
	SPREADSHEET = "spreadsheet",
}

export enum Mode {
	LIVE = "live",
	CACHED = "cached",
	DELAYED = "delayed",
}
